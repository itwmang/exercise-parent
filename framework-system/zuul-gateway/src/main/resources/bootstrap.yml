spring:
  cloud:
    config:
#      discovery:
#        enable: true
#        service-id: config-center
      uri: http://120.79.90.129:8888
      profile: @profileActive@
      label: master
      #名字可以写多个，逗号隔开
      name: zuul-gateway
      username: configcenter
      password: configcenter
      failFast: true
    bus:
      enabled: true
