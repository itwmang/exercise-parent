server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native #native访问本地配置 dev 打开git配置
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/local
#        git:
#          uri: https://github.com/itwmang/springCloudConfig
#          search-paths: mac-dev
#          username: itfast@yeah.net
#          password: itfast123.
    bus:
      trace:
        enabled: true
  rabbitmq:
     host: 127.0.0.1
     port: 5672
     username: rabbitmq
     password: rabbitmq
  security:
    user:
      name: configcenter
      password: config0510

eureka:
  client:
    service-url:
      defaultZone: http://localhost:2188/eureka

