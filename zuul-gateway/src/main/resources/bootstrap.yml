spring:
  application:
     name: zuul-gateway
  cloud:
    config:
      uri: http://configcenter:config0510@localhost:8888
      profile: @profileActive@
      label: master
      name: zuul-gateway
