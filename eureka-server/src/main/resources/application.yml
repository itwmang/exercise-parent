server:
  port: 2188
spring:
  application:
    name: eureka-server

eureka:
  instance  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
     defaultZone: http://localhost:2188/eureka/